<openerp>
    <data>
        <!-- Marketing Campaign -->

        <record id="act_marketing_campaign_segment_opened" model="ir.actions.act_window">
            <field name="res_model">marketing.campaign.segment</field>
            <field name="view_type">form</field>
            <field name="name">Segments</field>
            <field name="view_mode">tree,form</field>
            <field name="context">{'search_default_campaign_id': [active_id], 'default_campaign_id': active_id}</field>
        </record>

        <!-- Campaign Follow-ups -->

        <record id="act_marketing_campaign_campaign_followup" model="ir.actions.act_window">
            <field name="res_model">marketing.campaign.workitem</field>
            <field name="view_type">form</field>
            <field name="name">Workitems</field>
            <field name="view_mode">tree,form</field>
            <field name="context">{'search_default_campaign_id': [active_id], 'default_campaign_id': active_id}</field>
        </record>

        <!-- Campaign Statistics -->

        <record id="act_marketing_campaign_stat" model="ir.actions.act_window" >
            <field name="res_model">marketing.campaign.analysis</field>
            <field name="view_type">form</field>
            <field name="name">Campaign Follow-up</field>
            <field name="view_mode">tree,form</field>
            <field name="context">{'search_default_campaign_id': [active_id], 'default_campaign_id': active_id}</field>
        </record>
        <!-- Campaign Followups -->

        <record model="ir.ui.view" id="view_marketing_campaign_campaign_form">
            <field name="name">marketing.campaign.campaign.form</field>
            <field name="model">marketing.campaign.campaign</field>
            <field name="arch" type="xml">
                <form string="Lead Automation Campaign">
                    <header>
                        <button name="running" type="workflow" string="Run" states="draft" class="oe_highlight"/>
                        <button name="done" type="workflow" string="Done" states="running" class="oe_highlight"/>
                        <button name="draft" type="workflow" string="Set to Draft" states="done,cancelled"/>
                        <button name="cancelled" type="workflow" string="Cancel Campaign" states="running"/>
                        <field name="state" readonly="1" widget="statusbar" statusbar_visible="draft,running,done"/>
                    </header>
                    <sheet string="Campaign">
                        <div class="oe_right oe_button_box" name="button_box">
                            <button type="action"
                                    name="%(act_marketing_campaign_stat)d"
                                    class="oe_stat_button"
                                    icon="fa-keyboard-o"
                                    help="Campaign Statistics">
                                <div>Campaign<br/>Statistics</div>
                            </button>
                            <button type="action"
                                    name="%(act_marketing_campaign_segment_opened)d"
                                    class="oe_stat_button"
                                    icon="fa-table">
                                <field name="segments_count" widget="statinfo" string="Segments"/>
                            </button>
                            <button type="action"
                                    name="%(act_marketing_campaign_campaign_followup)d"
                                    class="oe_stat_button"
                                    icon="fa-thumbs-up"
                                    string="Follow-Up"/>$
                        </div>
                        <div class="oe_title">
                            <label for="name" class="oe_edit_only"/>
                            <h1><field name="name" class="oe_inline"/></h1>
                        </div>
                        <group>
                            <group>
                                <field name="mode"/>
                                <field name="fixed_cost"/>
                            </group>
                            <group>
                                <field name="object_id"/>
                                <field name="partner_field_id"/>
                                <field name="unique_field_id"/>
                            </group>
                        </group>
                        <separator string="Activities"/>
                        <field name="activity_ids" context="{'default_object_id': object_id, 'default_campaign_id': active_id}"/>
                    </sheet>
                </form>
            </field>
        </record>

        <record model="ir.ui.view" id="view_marketing_campaign_campaign_tree">
            <field name="name">marketing.campaign.campaign.tree</field>
            <field name="model">marketing.campaign.campaign</field>
            <field name="arch" type="xml">
                <tree colors="blue:state == 'draft';black:state == 'running';gray:state in ('done','cancelled')" string="Campaigns">
                    <field name="name"/>
                    <field name="object_id"/>
                    <field name="mode"/>
                    <!-- 					<field name="fixed_cost"/> -->
                    <field name="state"/>
                </tree>
            </field>
        </record>

        <record model="ir.actions.act_window" id="action_marketing_campaign_campaign_form">
            <field name="name">Campaigns</field>
            <field name="type">ir.actions.act_window</field>
            <field name="res_model">marketing.campaign.campaign</field>
            <field name="view_type">form</field>
            <field name="view_mode">tree,form</field>
            <field name="view_id" ref="view_marketing_campaign_campaign_tree"/>
            <field name="context">{'group_by': []}</field>
            <!-- 			<field name="search_view_id" ref="view_marketing_campaign_campaign_search"/> -->
            <field name="help" type="html">
                <p class="oe_view_nocontent_create">
                    Click to create a marketing campaign.
                </p><p>
                Odoo's marketing campaign allows you to automate communication
                to your prospects. You can define a segment (set of conditions) on
                your leads and partners to fullfil the campaign.
                </p><p>
                A campaign can have many activities like sending an email, printing
                a letter, assigning to a team, etc. These activities are triggered
                from specific situations; contact form, 10 days after first
                contact, if a lead is not closed yet, etc.
                </p>
            </field>
        </record>
    </data>
</openerp>
